[package]
name = "earthmesh_quality"
version = "0.1.0"
edition = "2021"
description = "Mesh quality metrics + reports (geometry/topology/refinement) for EarthMesh v3"
license = "MIT OR Apache-2.0"

[lib]
name = "earthmesh_quality"
path = "src/lib.rs"

[dependencies]
earthmesh_core = { path = "../earthmesh_core" }
earthmesh_geometry = { path = "../earthmesh_geometry" }
