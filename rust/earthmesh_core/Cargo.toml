[package]
name = "earthmesh_core"
version = "0.1.0"
edition = "2021"
description = "Typed Rust core constants and configuration for EarthMesh Fortran migration"
license = "MIT OR Apache-2.0"

[lib]
name = "earthmesh_core"
path = "src/lib.rs"

[dependencies]
