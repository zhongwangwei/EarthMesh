[package]
name = "earthmesh_core"
version = "3.0.0-alpha5"
edition = "2021"
description = "Typed Rust core constants and configuration for EarthMesh runtime architecture"
license = "GPL-2.0-only"
repository = "https://github.com/zhongwangwei/EarthMesh"

[lib]
name = "earthmesh_core"
path = "src/lib.rs"

[dependencies]
