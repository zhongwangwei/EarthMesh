[build-system]
requires = ["maturin>=1.13,<2.0"]
build-backend = "maturin"

[project]
name = "earthmesh-geometry"
dynamic = ["version"]
description = "Rust geometry kernels for EarthMesh v3"
requires-python = ">=3.10"

[tool.maturin]
features = ["extension-module"]
