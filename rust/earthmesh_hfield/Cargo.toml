[package]
name = "earthmesh_hfield"
version = "0.1.0"
edition = "2021"
description = "Unified mesh cell-width field h(x): criterion/region composition, spherical gradient limiting, and level quantization for EarthMesh refinement"
license = "MIT OR Apache-2.0"

[dependencies]

[dev-dependencies]
earthmesh_mesh = { path = "../earthmesh_mesh" }
